# Single source of truth for dependencies is pyproject.toml.
#   pip install -e '.[dev]'          # pricing, strategy, analysis + tests
#   pip install -e '.[dev,spark]'    # adds PySpark (requires Python <= 3.11)
-e .[dev]
